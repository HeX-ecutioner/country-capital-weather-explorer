{
    "name": "country-capital-weather-explorer",
    "version": "1.0.0",
    "description": "A single-page app using **HTML + CSS + vanilla JS** that: - Lets users search for a country (REST Countries API) - Shows country details (flag, name, capital, population) - Fetches current weather for the capital (via **Netlify serverless function + OpenWeatherMap**) - Includes responsive design, loading states, geolocation, and dark mode",
    "main": "config.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "dev": "netlify dev",
        "deploy": "netlify deploy"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "devDependencies": {
        "netlify-cli": "^23.5.0"
    }
}
